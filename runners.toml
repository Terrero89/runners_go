###################################################################################3

# Databaase configuration

# connection string is go pq driver format:
# host=<host> port=<port> user=<databaseUser>
# password=<password> dbname=<databaseUserPassword> dbname=<databaseName>

[Database]
connection_string = "host=localhost port=5432" user=postgres password=postgres dbname=runners_db sslmode=disable"
max_idle_connections = 5
max_open_connections = 20
connection_max_lifetime = "60s"
driver_name = "postgres"

###############################################################################

#3 HTTP server configuration

[http]

server_address = ":8080

####################################################################################